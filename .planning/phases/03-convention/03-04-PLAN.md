---
phase: 03-convention
plan: 4
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - "templates/НоваяКнопка.pushbutton/script.py"
  - "templates/НоваяКнопка.pushbutton/bundle.yaml"
  - "templates/НоваяКнопка.pushbutton/README.md"
  - tools/convention_baseline.json
  - "MM LAB.extension/MM Lab.tab/bundle.yaml"
autonomous: true
requirements: [CONV-STD, CONV-CHECK, CONV-REG]

must_haves:
  truths:
    - "templates/НоваяКнопка.pushbutton существует ВНЕ MM Lab.tab, pyRevit его не загружает (D-14)"
    - "Шаблон — минимальная рабочая кнопка: шапка → канонический бутстрап → revit_compat → Transaction → обработка ошибок → TaskDialog, с TODO-метками (D-13)"
    - "py -3 tools/check_convention.py 'templates/НоваяКнопка.pushbutton' --strict завершается с кодом 0"
    - "py -3 tools/check_convention.py --all --baseline tools/convention_baseline.json завершается с кодом 0 (legacy заморожен)"
    - "В tab bundle.yaml нет орфана «ВОР»"
  artifacts:
    - path: "templates/НоваяКнопка.pushbutton/script.py"
      provides: "рабочая кнопка-пример с каноническим бутстрапом (D-15) и TODO-метками"
      min_lines: 60
      contains: "require_supported_version"
    - path: "templates/НоваяКнопка.pushbutton/bundle.yaml"
      provides: "каркас title/tooltip/author c TODO"
      contains: "tooltip"
    - path: "templates/НоваяКнопка.pushbutton/README.md"
      provides: "каркас README по образцу «Мокрых зон» + раздел про icon.png"
      contains: "## Описание"
    - path: "tools/convention_baseline.json"
      provides: "grandfathered-список текущих кнопок (сгенерирован чекером из фактического аудита)"
      contains: "units"
  key_links:
    - from: "templates/НоваяКнопка.pushbutton/script.py"
      to: "MM LAB.extension/lib/revit_compat.py"
      via: "import revit_compat после канонического бутстрапа"
      pattern: "import revit_compat"
    - from: "tools/convention_baseline.json"
      to: "tools/check_convention.py"
      via: "флаг --baseline при прогоне --all"
      pattern: "units"
---

<objective>
Создать шаблон-скелет pushbutton в `templates/` (D-12..D-15), заморозить legacy-кнопки в `tools/convention_baseline.json` и привести полный прогон чекера по репо к зелёному (включая правку орфана «ВОР» в tab bundle.yaml).

Purpose: шаблон — стартовая точка для людей и `/mm-new-button`; baseline делает чекер полезным гейтом с первого дня (иначе он «красный» на 15 из 17 кнопок и бесполезен); зелёный `--all --baseline` — фазовый инвариант.
Output: `templates/НоваяКнопка.pushbutton/` (3 файла), `tools/convention_baseline.json`, исправленный `MM LAB.extension/MM Lab.tab/bundle.yaml`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-convention/03-CONTEXT.md
@.planning/phases/03-convention/03-PATTERNS.md

<interfaces>
<!-- Канонический lib-бутстрап (D-15) — ЕДИНСТВЕННАЯ допустимая форма; дословно в script.py шаблона. -->
```python
import os
import sys

_SCRIPT_DIR = os.path.dirname(__file__)
# pushbutton -> panel -> tab -> MM LAB.extension
_EXTENSION_DIR = os.path.normpath(os.path.join(_SCRIPT_DIR, "..", "..", ".."))
_LIB_DIR = os.path.join(_EXTENSION_DIR, "lib")
if os.path.isdir(_LIB_DIR) and _LIB_DIR not in sys.path:
    sys.path.insert(0, _LIB_DIR)
```
Путь рассчитан на рабочее положение кнопки ВНУТРИ `MM Lab.tab/<Панель>.panel/` (в templates/ он не работает — это норма, шаблон запускается только после копирования; зафиксировать комментарием в script.py).

<!-- Канон шапки (правила MM001–MM004). -->
```python
#! python3
# -*- coding: utf-8 -*-
"""Название кнопки  # TODO: замени

Что делает кнопка, кратко.  # TODO: опиши

Совместимость: Revit 2020 / 2022 / 2024
Зависимости: нет
"""
```

Публичный API compat, используемый шаблоном (из плана 03-02): `require_supported_version(command_name)`, `element_id_value(element_id)`.
Контракт чекера (из планов 03-01/03-03): `--strict` — гейт без baseline; кнопка вне `*.panel` не проверяется на MM007.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Шаблон-скелет templates/НоваяКнопка.pushbutton</name>
  <files>templates/НоваяКнопка.pushbutton/script.py, templates/НоваяКнопка.pushbutton/bundle.yaml, templates/НоваяКнопка.pushbutton/README.md</files>
  <read_first>
    - MM LAB.extension/MM Lab.tab/ИОС.panel/Сброс потерь.pushbutton/script.py (эталон структуры тела: COMMAND_NAME, Transaction Start/Commit в try, RollBack+raise в except, итоговый TaskDialog со счётчиками, верхнеуровневый show_error)
    - MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/Мокрые зоны.pushbutton/script.py (строки 1–40: канон шапки; main() + запуск — но БЕЗ голого except, там дефект MM011)
    - MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/Мокрые зоны.pushbutton/bundle.yaml (канон title/tooltip ru+en_us, author)
    - MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/Мокрые зоны.pushbutton/README.md (канон разделов README)
    - MM LAB.extension/lib/revit_compat.py (реальный API — вызовы шаблона должны совпадать с сигнатурами)
  </read_first>
  <action>
    Создать папку templates/НоваяКнопка.pushbutton в КОРНЕ репозитория (вне MM LAB.extension — D-14) с тремя файлами.
    script.py: шапка-канон из interfaces (+ __title__ = "Новая\nкнопка" и __author__ = "GENPRO LAB" с TODO-комментами); канонический бутстрап из interfaces дословно с комментарием про рабочее положение; импорты: clr + AddReference("RevitAPI")/("RevitAPIUI"), from Autodesk.Revit.DB import BuiltInCategory, FilteredElementCollector, Transaction; from Autodesk.Revit.UI import TaskDialog; import revit_compat; константа COMMAND_NAME = u"Новая кнопка" (# TODO); функция main(): (1) revit_compat.require_supported_version(COMMAND_NAME); (2) uidoc = __revit__.ActiveUIDocument, при None или None-документе — TaskDialog «Открой проект Revit и повтори команду.» и return; doc = uidoc.Document; (3) пример чтения: walls = FilteredElementCollector(doc).OfCategory(BuiltInCategory.OST_Walls).WhereElementIsNotElementType().ToElements(), count = revit_compat.iter_count(walls); (4) транзакционный блок-каркас: transaction = Transaction(doc, COMMAND_NAME); transaction.Start(); try: `# TODO: логика изменения модели (пример читает и НИЧЕГО не меняет)`; transaction.Commit(); except Exception: transaction.RollBack(); raise; (5) TaskDialog.Show(COMMAND_NAME, u"Стен в проекте: {0}".format(count)) — # TODO: замени отчёт; запуск: try: main() except SystemExit: pass (require_supported_version уже показал диалог) except Exception as ex: TaskDialog.Show(COMMAND_NAME, u"Ошибка:\n{0}".format(ex)). Голых except нет; TODO-метки — на каждом месте правки (D-13).
    bundle.yaml: структура «Мокрых зон» дословно — title (en_us: "New\nButton", ru: "Новая\nкнопка"), tooltip (en_us/ru), author: "GENPRO LAB"; значения снабдить комментами `# TODO: замени`.
    README.md: русские разделы: `# Новая кнопка (TODO: название)`, `## Описание` (TODO), `## Логика работы` (нумерованный список, TODO), `## Используемые параметры` (таблица-каркас), `## Зависимости` («нет; общие функции — MM LAB.extension/lib»), `## Совместимость` («Revit 2020 / 2022 / 2024»), `## Иконка` («положи рядом файл icon.png, PNG ~96×96; без него pyRevit покажет кнопку без иконки»), `## Как использовать шаблон` (скопируй папку в MM LAB.extension/MM Lab.tab/<Панель>.panel/, переименуй в <Имя>.pushbutton, заполни TODO, добавь имя кнопки в layout panel-bundle.yaml, сделай pyRevit Reload; или команда /mm-new-button).
    icon.png НЕ создавать (бинарный плейсхолдер не нужен — инструкция в README).
  </action>
  <verify>
    <automated>py -3 -m py_compile "templates/НоваяКнопка.pushbutton/script.py" && py -3 tools/check_convention.py "templates/НоваяКнопка.pushbutton" --strict</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m py_compile "templates/НоваяКнопка.pushbutton/script.py"` → exit 0
    - `py -3 tools/check_convention.py "templates/НоваяКнопка.pushbutton" --strict` → exit 0 (включая warning-правила: без мусора, без LookupParameter-литералов, без pyrevit.forms, docstring полный)
    - script.py содержит "import revit_compat", "require_supported_version", "Transaction", "RollBack", "TaskDialog" и не менее 5 меток "TODO"
    - script.py содержит строку `_EXTENSION_DIR = os.path.normpath(os.path.join(_SCRIPT_DIR, "..", "..", ".."))` (канонический бутстрап, 3 уровня вверх — НЕ 4, как в «Мокрых зонах») и НЕ содержит имени `EXTENSION_ROOT`
    - bundle.yaml содержит ключи title/tooltip с ru и en_us и author: "GENPRO LAB"
    - README.md содержит разделы "## Описание", "## Совместимость", "## Иконка", "## Как использовать шаблон"
    - Папки templates/ нет внутри MM LAB.extension (проверка: путь начинается с templates/ в корне репо)
  </acceptance_criteria>
  <done>Шаблон копируемый, рабочий, строгий чекер на нём зелёный</done>
</task>

<task type="auto">
  <name>Task 2: Правка tab bundle.yaml, генерация baseline, полный интеграционный прогон</name>
  <files>MM LAB.extension/MM Lab.tab/bundle.yaml, tools/convention_baseline.json</files>
  <read_first>
    - MM LAB.extension/MM Lab.tab/bundle.yaml (текущее состояние: орфан «ВОР» с хвостовыми пробелами, запись-разделитель «-----», финальная пустая строка; файл уже правился пользователем — сохранить все живые записи)
    - tools/check_convention.py (флаг --write-baseline и семантика baseline из плана 03-01)
    - .planning/phases/03-convention/03-VALIDATION.md (§Full suite command — целевой инвариант фазы)
  </read_first>
  <action>
    (1) Правка MM LAB.extension/MM Lab.tab/bundle.yaml: удалить запись layout «ВОР» ТОЛЬКО если папки "MM LAB.extension/MM Lab.tab/ВОР.panel" нет на диске (проверить перед правкой); убрать хвостовые пробелы у всех строк и финальную строку из одних пробелов; записи АРХИТЕКТУРА, ИОС, КООРДИНАЦИЯ и разделитель «-----» сохранить; файл завершить переводом строки; кодировка UTF-8 без BOM.
    (2) Сгенерировать baseline фактическим аудитом (НЕ переписывать цифры из RESEARCH — репо менялось): выполнить `py -3 tools/check_convention.py --all --write-baseline tools/convention_baseline.json`. Проверить содержимое: ключ units непустой; в units присутствуют записи для легаси-кнопок (как минимум кнопки без шебанга, например "IFC_Двери.pushbutton" в пути одного из ключей); шаблон templates/ в baseline НЕ попал (не входит в --all).
    (3) Инвариант: `py -3 tools/check_convention.py --all --baseline tools/convention_baseline.json` → exit 0.
    (4) Полный интеграционный прогон (full suite из VALIDATION): unittest discover всех тестов + py_compile compat и шаблона + strict-прогон шаблона.
    В коммит включить оба файла; сообщение: chore(03-04): baseline legacy-кнопок + чистка tab bundle.yaml.
  </action>
  <verify>
    <automated>py -3 -m unittest discover -s tools/tests -q && py -3 tools/check_convention.py --all --baseline tools/convention_baseline.json && py -3 tools/check_convention.py "templates/НоваяКнопка.pushbutton" --strict && py -3 -m py_compile "MM LAB.extension/lib/revit_compat.py" "templates/НоваяКнопка.pushbutton/script.py"</automated>
  </verify>
  <acceptance_criteria>
    - Составная команда verify завершается с кодом 0 целиком
    - `py -3 tools/check_convention.py --all` (БЕЗ baseline) завершается с кодом 1 — доказательство, что baseline действительно фильтрует реальные legacy-нарушения, а не пустой
    - tools/convention_baseline.json — валидный JSON с ключами "generated", "units"; в units есть хотя бы один ключ, содержащий ".pushbutton"
    - В MM LAB.extension/MM Lab.tab/bundle.yaml нет подстроки "ВОР" (при отсутствии папки ВОР.panel) и нет строк с хвостовыми пробелами
    - Записи "- АРХИТЕКТУРА", "- ИОС", "- КООРДИНАЦИЯ" в layout сохранены
  </acceptance_criteria>
  <done>Полный прогон чекера по репо зелёный с baseline; фазовый инвариант установлен</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| шаблонный код → модель Revit | скопированный шаблон будут запускать новички на рабочих моделях |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | T (Tampering) | templates/.../script.py | mitigate | пример read-only (подсчёт стен); транзакционный каркас с Commit в try и RollBack+raise в except; верхнеуровневый TaskDialog вместо тихого проглатывания |
| T-03-10 | D (DoS) | запуск шаблона на чужой версии Revit | mitigate | require_supported_version первой строкой main() (D-03) |
| T-03-11 | T (Tampering) | правка tab bundle.yaml | mitigate | удалять «ВОР» только после проверки отсутствия папки ВОР.panel; живые записи панелей сохраняются дословно (acceptance criteria) |
| T-03-12 | R (Repudiation) | baseline замораживает нарушения навсегда | accept | механизм снятия записи описан в /mm-adopt-script (план 03-06): при адаптации кнопки её запись удаляется из baseline |
</threat_model>

<verification>
- Полная команда из Task 2 verify → exit 0
- `git -c core.quotepath=false status --porcelain` показывает только запланированные файлы этого плана
</verification>

<success_criteria>
- Шаблон проходит --strict на 100%, содержит рабочий сценарий и TODO-метки
- Baseline сгенерирован из фактического аудита; --all --baseline зелёный; --all без baseline красный
- Орфан «ВОР» устранён; каноничный бутстрап зафиксирован единственной формой в шаблоне
</success_criteria>

## Artifacts this phase produces

Символы, создаваемые этим планом:
- Папка `templates/НоваяКнопка.pushbutton/` с файлами `script.py` (константа `COMMAND_NAME`, функция `main`), `bundle.yaml`, `README.md`
- Файл `tools/convention_baseline.json` (ключи `generated`, `note`, `units`)
- Изменённый `MM LAB.extension/MM Lab.tab/bundle.yaml` (удалена запись «ВОР», убраны хвостовые пробелы)

<output>
После завершения создать `.planning/phases/03-convention/03-04-SUMMARY.md` по шаблону summary.
</output>
