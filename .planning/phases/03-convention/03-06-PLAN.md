---
phase: 03-convention
plan: 6
type: execute
wave: 4
depends_on: ["03-04", "03-05"]
files_modified:
  - agents/commands/mm-adopt-script.md
  - agents/commands/mm-new-button.md
  - agents/commands/mm-check.md
  - agents/commands/mm-new-compat.md
  - agents/commands/mm-save-session.md
  - agents/commands/mm-update-repo.md
  - agents/commands/mm-doctor.md
autonomous: true
requirements: [CONV-ADAPT, CONV-REG, CONV-GSD, CONV-CHECK]

must_haves:
  truths:
    - "В agents/commands/ лежат 7 канонических процедур с префиксом mm- (D-19, D-20)"
    - "mm-adopt-script описывает полный поток: чекер → вопрос о панели с авто-подсказкой → адаптация → diff → ЯВНОЕ одобрение человека → регистрация в bundle.yaml → quick task GSD (D-08..D-11)"
    - "mm-save-session фиксирует утверждённый шаблон коммита, пофайловый стейджинг и push только с подтверждением (D-16..D-18)"
    - "mm-update-repo ограничен fetch + ff-only pull при чистом дереве"
    - "Процедуры самодостаточны: исполнимы любым из трёх агентов без Claude-специфичных инструментов (кроме явно помеченного пути /gsd-quick)"
  artifacts:
    - path: "agents/commands/mm-adopt-script.md"
      provides: "процедура приёмки/адаптации стороннего скрипта"
      min_lines: 80
      contains: "--strict"
    - path: "agents/commands/mm-save-session.md"
      provides: "процедура сессионного коммита + Obsidian"
      min_lines: 40
      contains: "core.quotepath=false"
    - path: "agents/commands/mm-new-button.md"
      provides: "скаффолд кнопки из templates/"
      contains: "НоваяКнопка.pushbutton"
    - path: "agents/commands/mm-doctor.md"
      provides: "self-check окружения и репозитория"
      contains: "SUPPORTED_VERSIONS"
  key_links:
    - from: "agents/commands/mm-adopt-script.md"
      to: "tools/check_convention.py"
      via: "гейт приёмки --strict (D-06)"
      pattern: "check_convention\\.py"
    - from: "agents/commands/mm-new-button.md"
      to: "templates/НоваяКнопка.pushbutton"
      via: "копирование скелета"
      pattern: "templates/"
    - from: "agents/commands/mm-adopt-script.md"
      to: ".planning/quick/"
      via: "создание GSD quick task (D-09)"
      pattern: "\\.planning/quick/"
---

<objective>
Написать 7 канонических процедур mm-команд в `agents/commands/` (D-19, D-20) — единственный полный текст каждой команды; адаптеры трёх агентов (план 03-07) будут лишь ссылаться на эти файлы.

Purpose: команда приёмки — вторая половина цели фазы (CONV-ADAPT/CONV-REG/CONV-GSD); остальные команды закрывают жизненный цикл: скаффолд, проверка, сессия, обновление, диагностика, расширение compat. Один канонический текст исключает дрейф между агентами.
Output: agents/commands/mm-adopt-script.md, mm-new-button.md, mm-check.md, mm-new-compat.md, mm-save-session.md, mm-update-repo.md, mm-doctor.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-convention/03-CONTEXT.md
@.planning/phases/03-convention/03-RESEARCH.md
@.planning/phases/03-convention/03-PATTERNS.md

<interfaces>
<!-- Общий формат канонической процедуры (все 7 файлов). -->
Каждый файл agents/commands/mm-<слаг>.md:
- `# /mm-<слаг> — <назначение одной строкой>`
- `## Аргументы` (что принимает; что спросить, если не переданы)
- `## Процедура` (нумерованные шаги с точными командами)
- `## Гейты и запреты` (что требует одобрения человека; что запрещено)
- `## Финал` (что сообщить пользователю)
Язык — русский; команды — точные, копируемые (py -3 …, git -c core.quotepath=false …).

<!-- Контракты, на которые опираются процедуры. -->
- Чекер: `py -3 tools/check_convention.py <путь|--all> [--strict] [--json] [--baseline tools/convention_baseline.json]`; exit 0/1/2; сырой .py поддержан
- Baseline: tools/convention_baseline.json, ключ units {relpath: [коды]}; при адаптации кнопки её запись удаляется
- Compat: MM LAB.extension/lib/revit_compat.py, SUPPORTED_VERSIONS = (2020, 2022, 2024)
- Шаблон: templates/НоваяКнопка.pushbutton/ (script.py, bundle.yaml, README.md)
- Panel layout: запись `- <Имя без .pushbutton>` в MM LAB.extension/MM Lab.tab/<Панель>.panel/bundle.yaml
- Quick task: папка .planning/quick/<id>-<slug>/ с <id>-PLAN.md (frontmatter: quick_id, status) и <id>-SUMMARY.md + строка в таблицу «Quick Tasks Completed» в .planning/STATE.md; id = YYMMDD-<3 буквы>
- Шаблон сессионного коммита: AGENTS.md §Git-регламент (дословно)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Процедура /mm-adopt-script (приёмка/адаптация стороннего скрипта)</name>
  <files>agents/commands/mm-adopt-script.md</files>
  <read_first>
    - AGENTS.md (правила, на которые процедура ссылается по MM-кодам; §Команды MM LAB)
    - tools/check_convention.py (docstring: точные флаги CLI и коды)
    - .planning/quick/260709-jko-get-parameter-typeerror-builtinparameter/260709-jko-PLAN.md (живой образец quick task артефактов)
    - .planning/phases/03-convention/03-RESEARCH.md (§Pattern 6 — правка layout; §Pattern 7 — механика quick task; §Common Pitfalls 4/6 — Reload, pyrevit.forms)
    - MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/IFC_Двери.pushbutton/script.py (эталонный «сторонний» вход — на нём процедура будет проходить UAT)
  </read_first>
  <action>
    Создать agents/commands/mm-adopt-script.md по формату interfaces. Шаги процедуры (зафиксировать именно эту последовательность):
    (1) Вход: путь к стороннему скрипту (.py) или папке; если не передан — спросить. Скрипт НИКОГДА не исполнять — только читать и анализировать (чекер статический).
    (2) Исходный отчёт: `py -3 tools/check_convention.py "<путь>" --json` (сырой режим) — сохранить список нарушений для дальнейшего diff-рассказа.
    (3) Панель (D-10): проанализировать содержимое и предложить панель с обоснованием — эвристика: Duct/Pipe/Mechanical/MEP/поток/система → ИОС; Room/Area/Wall/Door/Window/IFC/помещение → АРХИТЕКТУРА; иначе → КООРДИНАЦИЯ; задать пользователю вопрос «Куда ставим кнопку? Предлагаю <Панель> (почему)» и ЖДАТЬ ответа. Финальное слово — за человеком.
    (4) Имя кнопки: предложить из __title__/имени файла, согласовать с пользователем.
    (5) Адаптация (D-08, D-11): подготовить папку "MM LAB.extension/MM Lab.tab/<Панель>.panel/<Имя>.pushbutton/" по образцу templates/НоваяКнопка.pushbutton: script.py — перенести логику исходника, приведя к конвенции (шапка MM001–MM004; перезапись без BOM; канонический бутстрап; импорты по белому списку; pyrevit.forms → TaskDialog/WinForms-паттерны репо; LookupParameter("строка") → revit_compat.get_parameter/get_shared_parameter где применимо; голые except → except Exception; транзакции Start/Commit/RollBack; require_supported_version в начале main); bundle.yaml (title ru/en_us, tooltip, author); README.md (по разделам шаблона). Иконку исходника (*.png) перенести как icon.png, если есть.
    (6) Гейт-проверка: `py -3 tools/check_convention.py "<новая папка>" --strict` до exit 0.
    (7) РЕВЬЮ-ГЕЙТ (D-08, блокирующий): показать пользователю (а) сводку адаптационных правок по пунктам (что и почему изменено, с отсылкой к MM-кодам), (б) полный diff/содержимое новых файлов. ЖДАТЬ явного одобрения («да»/«одобряю»). Без одобрения — СТОП: ничего не регистрировать, файлы можно удалить по желанию пользователя.
    (8) Регистрация (D-11): дописать `- <Имя>` в layout файла "MM LAB.extension/MM Lab.tab/<Панель>.panel/bundle.yaml" (место в списке согласовать или в конец); если исходный скрипт уже лежал кнопкой в репо и есть в tools/convention_baseline.json — удалить его запись из units; контрольный прогон `py -3 tools/check_convention.py --all --baseline tools/convention_baseline.json` → exit 0.
    (9) Quick task GSD (D-09): в Claude Code — вызвать /gsd-quick с описанием приёмки; в Gemini/Kilo (нет GSD) — создать артефакты вручную по образцу .planning/quick/260709-jko-*: папка .planning/quick/<YYMMDD-xxx>-adopt-<слаг-кнопки>/, файл <id>-PLAN.md (frontmatter quick_id/status: complete; разделы Task / Adaptation summary), файл <id>-SUMMARY.md, строка в таблицу «Quick Tasks Completed» в .planning/STATE.md.
    (10) Коммит: пофайловый стейджинг ТОЛЬКО созданных/изменённых файлов (git add с явными путями в кавычках), сообщение на русском вида «приёмка: кнопка <Имя> адаптирована под конвенцию MM LAB»; push не делать без подтверждения.
    (11) Финал: напомнить пользователю сделать pyRevit Reload (или перезапустить Revit) и проверить кнопку на панели; предложить /mm-save-session, если сессия завершается.
    Гейты и запреты: не исполнять сторонний код; не регистрировать без одобрения (шаг 7); git add ./-A запрещён; git-команды с кириллическими путями — через `git -c core.quotepath=false` и пути в кавычках.
  </action>
  <verify>
    <automated>py -3 -c "import io,sys; t=io.open('agents/commands/mm-adopt-script.md',encoding='utf-8').read(); req=['--strict','--json','РЕВЬЮ-ГЕЙТ','bundle.yaml','convention_baseline.json','.planning/quick/','Reload','core.quotepath=false','не исполнять','/gsd-quick']; m=[r for r in req if r not in t]; sys.exit(0 if not m else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify завершается с кодом 0
    - Файл ≥ 80 строк; содержит разделы "## Аргументы", "## Процедура", "## Гейты и запреты", "## Финал"
    - Шаг ревью-гейта расположен ДО шага регистрации в bundle.yaml (по порядку нумерации в тексте)
    - Описаны оба пути quick task: /gsd-quick (Claude Code) и ручное создание .planning/quick/<id>/ (Gemini/Kilo)
    - Эвристика авто-подсказки панели перечисляет все три панели: ИОС, АРХИТЕКТУРА, КООРДИНАЦИЯ
  </acceptance_criteria>
  <done>Процедура приёмки полная: любой из трёх агентов может провести скрипт от проверки до регистрации и quick task, не нарушив гейтов D-08/D-10/D-17</done>
</task>

<task type="auto">
  <name>Task 2: Процедуры /mm-check, /mm-new-button, /mm-new-compat</name>
  <files>agents/commands/mm-check.md, agents/commands/mm-new-button.md, agents/commands/mm-new-compat.md</files>
  <read_first>
    - tools/check_convention.py (docstring — флаги и exit-коды для mm-check)
    - templates/НоваяКнопка.pushbutton/README.md (раздел «Как использовать шаблон» — mm-new-button автоматизирует ровно его)
    - MM LAB.extension/lib/revit_compat.py (структура веток и SUPPORTED_VERSIONS — mm-new-compat редактирует его)
    - tools/tests/test_revit_compat_contract.py (контрактный тест — mm-new-compat обязан его обновлять)
    - agents/commands/mm-adopt-script.md (формат из Task 1 — выдержать единый стиль)
  </read_first>
  <action>
    Создать три файла по формату interfaces:
    mm-check.md: аргументы — путь(и) к кнопке/скрипту, `--all`, `--strict`; процедура: выбрать команду (`py -3 tools/check_convention.py …`; для всего репо добавлять `--baseline tools/convention_baseline.json`; для новых/адаптируемых кнопок — `--strict`); выполнить; пересказать результат по-русски с группировкой по кнопкам и MM-кодам; при нарушениях — предложить исправления по каждому коду со ссылкой на AGENTS.md, ничего не править без запроса пользователя; трактовка exit-кодов 0/1/2.
    mm-new-button.md: аргументы — имя кнопки, панель (АРХИТЕКТУРА/ИОС/КООРДИНАЦИЯ; если не заданы — спросить, панель с авто-подсказкой как в mm-adopt-script); процедура: скопировать templates/НоваяКнопка.pushbutton в "MM LAB.extension/MM Lab.tab/<Панель>.panel/<Имя>.pushbutton"; заменить TODO: COMMAND_NAME, __title__, docstring, bundle.yaml title/tooltip, README-разделы (черновой текст согласовать с пользователем); дописать `- <Имя>` в layout panel-bundle.yaml; прогнать `py -3 tools/check_convention.py "<папка>" --strict` до зелёного; пофайловый коммит по согласованию; финал — напомнить про icon.png (~96×96) и pyRevit Reload.
    mm-new-compat.md: аргумент — новая версия Revit (например 2025); процедура: прочитать MM LAB.extension/lib/revit_compat.py; добавить версию в SUPPORTED_VERSIONS; проревизовать версионные ветки (карта единиц в _units_map, create_floor, make_element_id/element_id_value) на предмет новых ломающих изменений добавляемой версии (спросить пользователя/исследовать документацию What's New in Revit API; для 2025+ учесть .NET Core); обновить docstring модуля («Совместимость: …»), таблицу «Совместимость Revit» в AGENTS.md и кортеж в tools/tests/test_revit_compat_contract.py; прогнать `py -3 -m py_compile "MM LAB.extension/lib/revit_compat.py"` и `py -3 -m unittest discover -s tools/tests -p "test_revit_compat*.py" -q`; напомнить: изменения требуют Revit UAT на новой версии; коммит пофайловый.
  </action>
  <verify>
    <automated>py -3 -c "import io,sys; ok=True
for p,req in [('agents/commands/mm-check.md',['check_convention.py','--strict','--baseline']),('agents/commands/mm-new-button.md',['templates/','НоваяКнопка.pushbutton','layout','Reload','--strict']),('agents/commands/mm-new-compat.md',['SUPPORTED_VERSIONS','test_revit_compat','py_compile','AGENTS.md'])]:
    t=io.open(p,encoding='utf-8').read()
    ok=ok and all(r in t for r in req)
sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify завершается с кодом 0
    - Каждый файл содержит разделы "## Аргументы", "## Процедура", "## Финал"
    - mm-new-button.md содержит запрет создавать кнопку внутри templates/ и требование регистрации в layout
    - mm-new-compat.md перечисляет все три точки обновления: revit_compat.py, AGENTS.md, test_revit_compat_contract.py
  </acceptance_criteria>
  <done>Три процедуры исполнимы пошагово и согласованы с реальными артефактами планов 03-01..03-05</done>
</task>

<task type="auto">
  <name>Task 3: Процедуры /mm-save-session, /mm-update-repo, /mm-doctor</name>
  <files>agents/commands/mm-save-session.md, agents/commands/mm-update-repo.md, agents/commands/mm-doctor.md</files>
  <read_first>
    - AGENTS.md (§Git-регламент — шаблон коммита дословно; §Obsidian-хранилище — шаги заметок)
    - .planning/phases/03-convention/03-CONTEXT.md (§specifics — утверждённый шаблон коммита /mm-save-session; D-16..D-18)
    - .planning/phases/03-convention/03-RESEARCH.md (§Common Pitfalls 5 — кириллица в git; §Open Questions 3 — как doctor ищет установленные Revit)
    - agents/commands/mm-adopt-script.md (единый стиль)
  </read_first>
  <action>
    Создать три файла по формату interfaces:
    mm-save-session.md (D-16..D-18, D-21): процедура: (1) определить файлы ТЕКУЩЕЙ сессии — только созданные/изменённые самим агентом в этой сессии (вести список по ходу работы; сверить с `git -c core.quotepath=false status --porcelain`, НО не включать чужие/старые изменения — при сомнении спросить пользователя по каждому спорному файлу); (2) Obsidian-поток: создать заметку MMLabs_OBSIDIAN/sessions/<YYYY-MM-DD>-<тема>.md (что сделано, ссылки), обновить «текущие приоритеты.md», при принятых решениях — knowledge/decisions/, при разобранных багах — knowledge/debugging/, обновить 00-home/index.md при новых заметках; заметки сессии тоже входят в коммит; (3) если файлов нет — коммит НЕ создавать, сообщить и закончить; (4) стейджинг ПОФАЙЛОВО: `git add "<путь>"` для каждого файла (пути в кавычках; git add . и -A запрещены); (5) коммит с сообщением ДОСЛОВНО по шаблону из AGENTS.md §Git-регламент (заголовок «сессия: …», разделы «## Сессия» (Агент/модель, Дата YYYY-MM-DD, Изменено файлов N), «## Изменённые файлы» (полный список), «## Результаты»); Агент/модель заполнять фактическим именем агента и модели; (6) показать `git log -1 --stat`; (7) спросить «Сделать push?» — push ТОЛЬКО после явного «да» (D-18), иначе закончить на локальном коммите.
    mm-update-repo.md: процедура: (1) `git -c core.quotepath=false status --porcelain` — если вывод НЕ пуст, СТОП: показать список и предложить сначала /mm-save-session (или явное решение пользователя, что делать с изменениями); (2) `git fetch origin`; (3) `git pull --ff-only`; (4) если ff-only невозможен (расхождение истории) — СТОП, показать `git status` и `git log --oneline -5 HEAD..origin/<ветка>`, решает человек; (5) сообщить итог (обновлено/уже актуально) и напомнить про pyRevit Reload, если менялись MM LAB.extension или lib. Запреты: reset --hard, clean, checkout --force, rebase без явного запроса пользователя.
    mm-doctor.md: диагностика без правок (read-only, правки только по явному запросу): (1) версии Revit: перечислить папки "C:\Program Files\Autodesk\Revit 20*" (и/или `reg query "HKLM\SOFTWARE\Autodesk\Revit"` в try) и сравнить с SUPPORTED_VERSIONS из MM LAB.extension/lib/revit_compat.py — предупредить о неподдерживаемых установках; (2) целостность vendored: папки lib/openpyxl и lib/et_xmlfile существуют; (3) конвенция: `py -3 tools/check_convention.py --all --baseline tools/convention_baseline.json` (+ `py -3 -m unittest discover -s tools/tests -q`); (4) обязательные файлы: AGENTS.md, templates/НоваяКнопка.pushbutton/, agents/commands/ (7 файлов); (5) git: текущая ветка, чистота дерева, `git fetch --dry-run` доступность origin (в try — офлайн допустим); (6) итоговый отчёт по-русски таблицей «Проверка / Статус / Что делать».
  </action>
  <verify>
    <automated>py -3 -c "import io,sys; ok=True
for p,req in [('agents/commands/mm-save-session.md',['сессия:','## Сессия','Агент/модель','core.quotepath=false','git add','push']),('agents/commands/mm-update-repo.md',['--ff-only','status --porcelain','reset --hard']),('agents/commands/mm-doctor.md',['SUPPORTED_VERSIONS','openpyxl','--baseline','AGENTS.md'])]:
    t=io.open(p,encoding='utf-8').read()
    ok=ok and all(r in t for r in req)
sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify завершается с кодом 0
    - mm-save-session.md: шаблон коммита совпадает с AGENTS.md §Git-регламент (заголовок «сессия:», разделы «## Сессия», «## Изменённые файлы», «## Результаты»); есть явные запреты git add . и git add -A; push описан строго после подтверждения
    - mm-update-repo.md: единственный способ обновления — fetch + pull --ff-only; при грязном дереве процедура останавливается; деструктивные команды перечислены как запрещённые
    - mm-doctor.md: диагностика описана как read-only; перечислены все 6 групп проверок
    - Все 7 файлов agents/commands/mm-*.md существуют (итог Task 1–3)
    - Коммит: docs(03-06): канонические процедуры mm-команд
  </acceptance_criteria>
  <done>Каталог из 7 канонических процедур полон; Git-регламент и Obsidian-поток исполняются командой, а не памятью агента</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| сторонний скрипт → процедура приёмки | недоверенный код проходит через адаптацию в рабочее расширение |
| процедуры → git-репозиторий | команды делают коммиты/pull от имени пользователя |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | E (Elevation) | mm-adopt-script | mitigate | процедура запрещает исполнение принимаемого кода; анализ только чекером (ast) и чтением; запуск адаптированной кнопки — человеком в Revit после ревью |
| T-03-17 | A (Access control, D-08) | mm-adopt-script шаг регистрации | mitigate | блокирующий ревью-гейт: diff → явное одобрение → только затем bundle.yaml/коммит (acceptance criteria проверяют порядок шагов) |
| T-03-18 | T (Tampering) | git-вызовы с кириллическими путями | mitigate | все процедуры: пути в кавычках, `git -c core.quotepath=false`, пофайловый стейджинг, запрет git add ./-A (D-17) |
| T-03-19 | R/T (Repudiation/Tampering) | mm-update-repo | mitigate | только fetch + pull --ff-only при чистом дереве; reset --hard/clean/checkout --force запрещены текстом процедуры |
| T-03-20 | T (Tampering) | mm-doctor | mitigate | диагностика read-only; никаких правок без явного запроса пользователя |
</threat_model>

<verification>
- Все три automated-команды задач → exit 0
- `ls agents/commands` показывает ровно 7 файлов mm-*.md
- Слаги файлов совпадают с каталогом в AGENTS.md §Команды MM LAB

Manual UAT (фазовый гейт, интерактивно):
- [ ] Прогнать /mm-adopt-script (через Claude-адаптер после плана 03-07) на MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/IFC_Двери.pushbutton: чекер отработал, панель спрошена, diff показан, без «да» регистрации нет
- [ ] /mm-save-session создаёт коммит по шаблону и НЕ пушит без подтверждения
</verification>

<success_criteria>
- 7 процедур в едином формате, самодостаточных для Claude Code / Gemini CLI / Kilo Code
- Гейты D-08 (одобрение), D-10 (панель), D-17 (пофайловый стейджинг), D-18 (push с подтверждением) зафиксированы текстом, а не подразумеваются
- Quick task при приёмке описан для обоих миров: /gsd-quick и ручные артефакты
</success_criteria>

## Artifacts this phase produces

Символы, создаваемые этим планом:
- Каталог `agents/commands/` с 7 файлами: `mm-adopt-script.md`, `mm-new-button.md`, `mm-check.md`, `mm-new-compat.md`, `mm-save-session.md`, `mm-update-repo.md`, `mm-doctor.md`
- Структура процедуры: разделы «## Аргументы», «## Процедура», «## Гейты и запреты», «## Финал»
- Идентификаторы команд (слаги): mm-adopt-script, mm-new-button, mm-check, mm-save-session, mm-update-repo, mm-doctor, mm-new-compat

<output>
После завершения создать `.planning/phases/03-convention/03-06-SUMMARY.md` по шаблону summary.
</output>
