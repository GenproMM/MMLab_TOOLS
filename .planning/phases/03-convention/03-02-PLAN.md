---
phase: 03-convention
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - "MM LAB.extension/lib/revit_compat.py"
  - tools/tests/test_revit_compat_contract.py
autonomous: true
requirements: [CONV-STD]

must_haves:
  truths:
    - "В MM LAB.extension/lib существует revit_compat.py — единый compat-модуль (D-01), компилируемый py_compile"
    - "SUPPORTED_VERSIONS == (2020, 2022, 2024) (D-02)"
    - "require_supported_version показывает TaskDialog со списком поддержанных версий и завершает скрипт на неподдерживаемой версии (D-03)"
    - "Модуль закрывает Units 2021+, Floor.Create 2023, ElementId Int64 2024 и pythonnet-обходы (D-04)"
    - "Контрактный тест (ast, без Revit) подтверждает шапку, версии и полный публичный API"
  artifacts:
    - path: "MM LAB.extension/lib/revit_compat.py"
      provides: "стабильные хелперы поверх версионного Revit API"
      min_lines: 200
      contains: "SUPPORTED_VERSIONS = (2020, 2022, 2024)"
    - path: "tools/tests/test_revit_compat_contract.py"
      provides: "AST-контроль публичного API без импорта Revit"
      min_lines: 50
      contains: "require_supported_version"
  key_links:
    - from: "tools/tests/test_revit_compat_contract.py"
      to: "MM LAB.extension/lib/revit_compat.py"
      via: "ast.parse исходника (модуль не импортируется — clr недоступен локально)"
      pattern: "ast\\.parse"
    - from: "MM LAB.extension/lib/revit_compat.py"
      to: "builtins.__revit__"
      via: "каскад детекции версии"
      pattern: "getattr\\(builtins"
---

<objective>
Создать `MM LAB.extension/lib/revit_compat.py` — единственное место версионных ветвлений Revit API (D-01..D-04): детекция версии, fail-fast, канонический каскад get_Parameter, ElementId Int64, Units 2020↔2022+, Floor.Create, pythonnet-interop. Плюс контрактный тест, проверяющий публичный API через ast без Revit.

Purpose: скрипты зовут стабильные хелперы, а не сырой версионный API; `if version >=` вне compat запрещены конвенцией. Шаблон кнопки (план 03-04) и AGENTS.md (план 03-05) ссылаются на этот API.
Output: `MM LAB.extension/lib/revit_compat.py`, `tools/tests/test_revit_compat_contract.py`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-convention/03-CONTEXT.md
@.planning/phases/03-convention/03-RESEARCH.md
@.planning/phases/03-convention/03-PATTERNS.md

<interfaces>
<!-- ПУБЛИЧНЫЙ API compat (фиксированный контракт для планов 03-04, 03-05, 03-06). -->

```python
SUPPORTED_VERSIONS = (2020, 2022, 2024)

def get_revit_version(revit=None):            # -> int | None; каскад: аргумент -> builtins.__revit__ -> pyrevit.HOST_APP -> None
def require_supported_version(command_name, revit=None):  # -> int; при None/неподдерживаемой: TaskDialog + raise SystemExit (D-03)

# --- параметры (D-04: pythonnet-обходы; консолидация двух существующих обходов репо) ---
def get_parameter(element, built_in_parameter, *fallback_names):   # -> Parameter | None
def get_shared_parameter(element, guid):      # guid: str | System.Guid -> Parameter | None

# --- ElementId: 2024 = Int64 .Value ---
def element_id_value(element_id):             # -> int; None -> -1; .Value -> fallback .IntegerValue
def make_element_id(id_value):                # -> ElementId

# --- Units: 2020 = DisplayUnitType, 2022/2024 = ForgeTypeId/UnitTypeId ---
def convert_from_internal(value, unit_key):   # unit_key: "mm" | "cm" | "m" | "m2" | "m3"
def convert_to_internal(value, unit_key):

# --- Floor: 2020 = doc.Create.NewFloor, 2022+ = Floor.Create(IList[CurveLoop]) ---
def create_floor(doc, curve_loops, floor_type_id, level_id):  # транзакцию НЕ открывает — обязанность вызывающего

# --- pythonnet interop ---
def to_net_list(items, net_type):             # -> System.Collections.Generic.List[net_type]
def enum_from_int(enum_type, int_value):      # Enum.ToObject
def iter_count(sequence):                     # безопасный подсчёт .NET IList / py-последовательностей (кейс коммита 3fcf888)

# --- vendored lib (корневой lib/ с openpyxl) ---
def ensure_vendor_lib():                      # -> str | None; добавляет <repo>/lib в sys.path (для vendored openpyxl/et_xmlfile)
```

Канонический каскад get_parameter (выбран из двух существующих обходов, третий вариант НЕ создавать):
1. `element.get_Parameter(built_in_parameter)` в try/except (TypeError на pythonnet 3.x);
2. `element.get_Parameter.__overloads__[BuiltInParameter](built_in_parameter)` (обход «Мокрых зон»);
3. BIP -> имя через `document.GetElement(ElementId(int(bip)))` + `GetDefinition().Name` с кешем `_BIP_NAME_CACHE` -> `element.LookupParameter(name)` (обход ios_common_helpers);
4. `element.LookupParameter(name)` по каждому из fallback_names;
5. None.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать MM LAB.extension/lib/revit_compat.py</name>
  <files>MM LAB.extension/lib/revit_compat.py</files>
  <read_first>
    - MM LAB.extension/lib/ios_common_helpers.py (строки 1–110: стиль шапки/импортов; element_id_value — скопировать семантику дословно, включая None -> -1; строки 145–182: обход BIP->имя с кешем)
    - MM LAB.extension/MM Lab.tab/АРХИТЕКТУРА.panel/Мокрые зоны.pushbutton/script.py (строки 80–96: обход __overloads__)
    - .planning/phases/03-convention/03-RESEARCH.md (§Pattern 3 — скелет модуля; §Code Examples — проверенные сигнатуры Units/ElementId/Floor.Create; §Common Pitfalls 3 — __revit__ в lib негарантирован)
    - .planning/phases/03-convention/03-PATTERNS.md (§revit_compat.py — точные выдержки кода-образца)
  </read_first>
  <action>
    Создать модуль по контракту interfaces. Обязательные свойства:
    (1) Шапка: строка 1 `#! python3`, строка 2 `# -*- coding: utf-8 -*-`, русский docstring с назначением, строками «Совместимость: Revit 2020 / 2022 / 2024» и «Зависимости: нет» и коротким примером использования (require_supported_version + get_parameter).
    (2) Импорты по стилю ios_common_helpers: stdlib (builtins, os, sys) -> clr + clr.AddReference("RevitAPI")/("RevitAPIUI") -> Autodesk.Revit.DB (BuiltInParameter, ElementId, UnitUtils и т.п. — по одному на строку, алфавитно) -> Autodesk.Revit.UI (TaskDialog) -> System.
    (3) Никаких версионных вычислений на уровне модуля, которые могут упасть при импорте: карта единиц Units строится лениво в приватной функции `_units_map()` при первом вызове convert_* (ветвление РОВНО одно: версия <= 2020 -> DisplayUnitType.DUT_MILLIMETERS/DUT_CENTIMETERS/DUT_METERS/DUT_SQUARE_METERS/DUT_CUBIC_METERS; иначе UnitTypeId.Millimeters/Centimeters/Meters/SquareMeters/CubicMeters; сигнатура UnitUtils.ConvertFromInternalUnits(value, unit) одинакова — меняется тип unit).
    (4) get_revit_version: каскад — явный аргумент revit -> getattr(builtins, "__revit__", None) -> try import pyrevit.HOST_APP (в try/except ImportError); из объекта версия через .Application.VersionNumber либо .VersionNumber (оба варианта в try), int(...); при неудаче None. НЕ полагаться на глобальный __revit__ без builtins-фолбэка (Pitfall 3).
    (5) require_supported_version(command_name, revit=None): версия None или не в SUPPORTED_VERSIONS -> TaskDialog.Show(command_name, русское сообщение вида «Эта кнопка поддерживает Revit 2020 / 2022 / 2024. Текущая версия: {X или "не определена"}. Обратись в GENPRO LAB.») и raise SystemExit; иначе вернуть int.
    (6) get_parameter — канонический каскад из interfaces (5 ступеней, кеш _BIP_NAME_CACHE на уровне модуля); get_shared_parameter — принять str или System.Guid (str оборачивать в System.Guid), вызвать element.get_Parameter(guid) с тем же __overloads__-фолбэком.
    (7) element_id_value — семантика ios_common_helpers дословно (None -> -1; try .Value except -> .IntegerValue); make_element_id(id_value) — try ElementId(System.Int64(id_value)) except -> ElementId(int(id_value)).
    (8) create_floor(doc, curve_loops, floor_type_id, level_id): версия <= 2020 -> собрать CurveArray из ПЕРВОГО CurveLoop (цикл по loop, curve_array.Append(curve)), floor_type = doc.GetElement(floor_type_id), level = doc.GetElement(level_id), вернуть doc.Create.NewFloor(curve_array, floor_type, level, False); иначе -> Floor.Create(doc, to_net_list(curve_loops, CurveLoop), floor_type_id, level_id). В docstring зафиксировать: транзакцию открывает вызывающий; ветка 2020 не поддерживает отверстия (только первый контур).
    (9) to_net_list через System.Collections.Generic.List[net_type]; enum_from_int через System.Enum.ToObject(enum_type, int_value); iter_count: try len(sequence) -> except TypeError: try sequence.Count -> except: подсчёт итерацией.
    (10) ensure_vendor_lib(): repo_root = normpath(join(dirname(__file__), "..", "..")) (lib -> MM LAB.extension -> корень репо), vendor = join(repo_root, "lib"); если папка существует и нет в sys.path — sys.path.append(vendor); вернуть путь или None. Docstring: «vendored-библиотеки (openpyxl, et_xmlfile); вызывать после бутстрапа, только если кнопке нужен vendored-пакет».
    (11) Все docstring — русские; никаких голых except (только except Exception или конкретные типы); модуль не открывает транзакций и не пишет в модель.
  </action>
  <verify>
    <automated>py -3 -m py_compile "MM LAB.extension/lib/revit_compat.py"</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m py_compile "MM LAB.extension/lib/revit_compat.py"` завершается с кодом 0
    - Файл содержит строку `SUPPORTED_VERSIONS = (2020, 2022, 2024)` и все 13 публичных имён из interfaces (get_revit_version, require_supported_version, get_parameter, get_shared_parameter, element_id_value, make_element_id, convert_from_internal, convert_to_internal, create_floor, to_net_list, enum_from_int, iter_count, ensure_vendor_lib)
    - Строка 1 файла == `#! python3`, строка 2 == `# -*- coding: utf-8 -*-`, файл без UTF-8 BOM
    - В файле нет голого `except:` (только `except Exception` или конкретные типы) и нет `lookupParameter` с малой буквы
    - Docstring модуля содержит «Совместимость: Revit 2020 / 2022 / 2024» и «Зависимости: нет»
  </acceptance_criteria>
  <done>Модуль создан, компилируется, публичный API полон, версионные if существуют только внутри compat</done>
</task>

<task type="auto">
  <name>Task 2: Контрактный тест API compat (ast, без Revit)</name>
  <files>tools/tests/test_revit_compat_contract.py</files>
  <read_first>
    - MM LAB.extension/lib/revit_compat.py (созданный в Task 1 — тест проверяет именно его)
    - .planning/phases/03-convention/03-VALIDATION.md (§Test Infrastructure — команды запуска)
  </read_first>
  <action>
    Создать tools/tests/test_revit_compat_contract.py (stdlib unittest). Модуль compat импортировать НЕЛЬЗЯ (import clr недоступен вне Revit) — тест читает исходник и анализирует ast. Путь к модулю: Path(__file__).resolve().parents[2] / "MM LAB.extension" / "lib" / "revit_compat.py". Тесты:
    (1) test_header: первые 3 байта != BOM; строка 1 == "#! python3"; строка 2 == "# -*- coding: utf-8 -*-".
    (2) test_docstring: ast.get_docstring содержит "Совместимость: Revit 2020 / 2022 / 2024" и "Зависимости: нет".
    (3) test_supported_versions: в ast найден Assign для SUPPORTED_VERSIONS со значением-кортежем (2020, 2022, 2024) (через ast.literal_eval узла).
    (4) test_public_api: множество имён top-level FunctionDef включает все 13 функций контракта (перечислить список PUBLIC_API в тесте дословно).
    (5) test_no_bare_except: в ast.walk нет ExceptHandler с type is None.
    (6) test_compiles: py_compile.compile(path, doraise=True) не бросает.
    ВАЖНО: имя файла теста начинается с test_revit_compat — команды прогонов других планов используют шаблоны discovery по имени, конфликтов с test_check_convention*.py быть не должно.
  </action>
  <verify>
    <automated>py -3 -m unittest discover -s tools/tests -p "test_revit_compat*.py" -q</automated>
  </verify>
  <acceptance_criteria>
    - `py -3 -m unittest discover -s tools/tests -p "test_revit_compat*.py" -q` завершается с кодом 0
    - Тест-файл содержит список PUBLIC_API из 13 имён и строку "ast.parse"
    - Прогон НЕ импортирует revit_compat (в тесте нет `import revit_compat`)
    - Коммит: feat(03-02): revit_compat + контрактный тест API
  </acceptance_criteria>
  <done>Контрактный тест зелёный и защищает публичный API compat от случайного дрейфа</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| скрипт кнопки → compat → Revit API | compat — прослойка между кодом кнопок и моделью Revit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T (Tampering) | create_floor и будущие пишущие хелперы | mitigate | compat НЕ открывает транзакций (докстринги фиксируют: транзакция — обязанность вызывающего); модуль не пишет в модель сам |
| T-03-06 | D (DoS) | запуск на неподдерживаемой версии Revit | mitigate | require_supported_version: TaskDialog с перечнем версий + SystemExit вместо невнятного падения API (D-03) |
| T-03-07 | I (Info disclosure) | текст TaskDialog | accept | сообщения содержат только версию Revit и имя команды |
</threat_model>

<verification>
- `py -3 -m py_compile "MM LAB.extension/lib/revit_compat.py"` → exit 0
- `py -3 -m unittest discover -s tools/tests -p "test_revit_compat*.py" -q` → exit 0

Manual UAT (Revit, follow-up вне headless-среды — прецедент Phase 1; выполняется на фазовом гейте):
- [ ] Revit 2020: кнопка-пример из templates (после плана 03-04) запускается, require_supported_version пропускает, convert_from_internal(1000, "mm") возвращает число
- [ ] Revit 2022: то же
- [ ] Revit 2024: то же; element_id_value возвращает int для стены
- [ ] Любая иная версия (при наличии): TaskDialog «поддерживает 2020 / 2022 / 2024» и мягкий выход
</verification>

<success_criteria>
- Все версионные ветвления фазы живут только в revit_compat.py
- Публичный API из 13 функций + SUPPORTED_VERSIONS зафиксирован контрактным тестом
- Канонический каскад get_parameter консолидирует оба существующих обхода (третьего варианта в репо не появилось)
</success_criteria>

## Artifacts this phase produces

Символы, создаваемые этим планом:
- Файл `MM LAB.extension/lib/revit_compat.py`: константа `SUPPORTED_VERSIONS`; функции `get_revit_version`, `require_supported_version`, `get_parameter`, `get_shared_parameter`, `element_id_value`, `make_element_id`, `convert_from_internal`, `convert_to_internal`, `create_floor`, `to_net_list`, `enum_from_int`, `iter_count`, `ensure_vendor_lib`; приватные `_BIP_NAME_CACHE`, `_units_map`
- Файл `tools/tests/test_revit_compat_contract.py`: константа `PUBLIC_API`, класс TestCase с тестами test_header, test_docstring, test_supported_versions, test_public_api, test_no_bare_except, test_compiles

<output>
После завершения создать `.planning/phases/03-convention/03-02-SUMMARY.md` по шаблону summary.
</output>
