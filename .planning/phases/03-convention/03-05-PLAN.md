---
phase: 03-convention
plan: 5
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - AGENTS.md
  - CLAUDE.md
  - GEMINI.md
  - .kilocode/rules/00-mmlab.md
autonomous: true
requirements: [CONV-STD]

must_haves:
  truths:
    - "AGENTS.md в корне — единственный полный текст конвенции на русском (D-05, D-07)"
    - "CLAUDE.md начинается с @AGENTS.md и содержит только Claude-специфику; текст конвенции в нём НЕ дублируется"
    - "GEMINI.md и .kilocode/rules/00-mmlab.md — тонкие указатели на AGENTS.md"
    - "graphify-правило и Obsidian-поток «сохрани сессию» перенесены в AGENTS.md (D-21)"
    - "userEmail/currentDate в AGENTS.md отсутствуют (D-21)"
  artifacts:
    - path: "AGENTS.md"
      provides: "канонический стандарт скриптов MM LAB"
      min_lines: 180
      contains: "## Обязательные правила кода"
    - path: "CLAUDE.md"
      provides: "тонкий указатель + Claude-специфика (GSD)"
      contains: "@AGENTS.md"
    - path: "GEMINI.md"
      provides: "тонкий указатель для Gemini CLI"
      contains: "@AGENTS.md"
    - path: ".kilocode/rules/00-mmlab.md"
      provides: "указатель для старых версий Kilo Code (новые читают AGENTS.md нативно)"
      contains: "AGENTS.md"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "механизм @import Claude Code (строка 1)"
      pattern: "^@AGENTS\\.md"
    - from: "AGENTS.md"
      to: "tools/check_convention.py"
      via: "раздел «Проверка конвенции» с CLI-командами"
      pattern: "check_convention"
    - from: "AGENTS.md"
      to: "agents/commands/"
      via: "каталог 7 mm-команд со ссылками на канонические процедуры"
      pattern: "agents/commands/mm-"
---

<objective>
Написать `AGENTS.md` — канонический стандарт написания скриптов MM LAB (русский, для новичков и трёх ИИ-агентов) — и превратить `CLAUDE.md` в тонкий указатель, добавив `GEMINI.md` и `.kilocode/rules/00-mmlab.md` (D-05, D-07, D-21).

Purpose: один источник правды вместо дублей; Claude Code читает через @import, Gemini через memory import, Kilo — AGENTS.md нативно. Стандарт ссылается на реальные артефакты фазы: чекер, compat, шаблон, mm-команды.
Output: AGENTS.md, переписанный CLAUDE.md, GEMINI.md, .kilocode/rules/00-mmlab.md.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-convention/03-CONTEXT.md
@.planning/phases/03-convention/03-RESEARCH.md

<interfaces>
<!-- Фиксированные имена для перекрёстных ссылок в AGENTS.md (контракты планов 03-01..03-04, 03-06). -->
- Чекер: `py -3 tools/check_convention.py [пути|--all] [--strict] [--json] [--baseline tools/convention_baseline.json]`; правила MM000–MM014; baseline = tools/convention_baseline.json
- Compat: `MM LAB.extension/lib/revit_compat.py` — SUPPORTED_VERSIONS (2020, 2022, 2024), require_supported_version, get_parameter, get_shared_parameter, element_id_value, make_element_id, convert_from_internal, convert_to_internal, create_floor, to_net_list, enum_from_int, iter_count, ensure_vendor_lib
- Шаблон: `templates/НоваяКнопка.pushbutton/` (script.py + bundle.yaml + README.md; icon.png кладёт автор)
- Команды (план 03-06, слаги фиксированы): /mm-adopt-script, /mm-new-button, /mm-check, /mm-save-session, /mm-update-repo, /mm-doctor, /mm-new-compat — канонические процедуры в agents/commands/mm-<слаг>.md
- Тесты: `py -3 -m unittest discover -s tools/tests -q`

<!-- Канонический бутстрап (дословно тот же блок, что в шаблоне, план 03-04). -->
```python
import os
import sys

_SCRIPT_DIR = os.path.dirname(__file__)
# pushbutton -> panel -> tab -> MM LAB.extension
_EXTENSION_DIR = os.path.normpath(os.path.join(_SCRIPT_DIR, "..", "..", ".."))
_LIB_DIR = os.path.join(_EXTENSION_DIR, "lib")
if os.path.isdir(_LIB_DIR) and _LIB_DIR not in sys.path:
    sys.path.insert(0, _LIB_DIR)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Написать AGENTS.md — канонический стандарт</name>
  <files>AGENTS.md</files>
  <read_first>
    - CLAUDE.md (корневой — текущие правила graphify, GSD Release Map, Obsidian-поток: что переносится в AGENTS.md, что остаётся)
    - tools/check_convention.py (docstring и словарь RULES — таблица правил в AGENTS.md обязана до буквы совпадать с реализацией)
    - MM LAB.extension/lib/revit_compat.py (реальный публичный API для раздела «Совместимость Revit»)
    - .planning/phases/03-convention/03-CONTEXT.md (D-01..D-21 и §specifics: шаблон коммита, формулировка vendored-исключения)
    - .planning/phases/03-convention/03-RESEARCH.md (§Pattern 1 — механика указателей; §Pattern 6 — правила layout; §Common Pitfalls — BOM, Reload, pyrevit.forms, консервативный синтаксис)
  </read_first>
  <action>
    Создать AGENTS.md в корне репозитория (русский язык; код, имена Revit API и файлов — английские/как в репо; целевой размер 200–300 строк). Обязательные разделы и содержание:
    (1) `# AGENTS.md — стандарт скриптов MM LAB`: назначение, аудитория (новички без опыта Python/Revit API; ИИ-агенты Claude Code / Gemini CLI / Kilo Code), статус — единственный источник правды, per-agent файлы только ссылаются сюда.
    (2) `## Архитектура репозитория`: дерево с пояснениями — MM LAB.extension/MM Lab.tab/<Панель>.panel/<Кнопка>.pushbutton; first-party lib = MM LAB.extension/lib (автоматически в sys.path силами pyRevit; правки lib требуют pyRevit Reload); vendored lib = корневой lib/ (openpyxl, et_xmlfile — подключается только через revit_compat.ensure_vendor_lib); templates/; tools/; agents/commands/.
    (3) `## Обязательные правила кода` — нумерованный список с MM-кодами чекера: шапка (точный блок: `#! python3` строка 1, `# -*- coding: utf-8 -*-` строка 2, русский docstring с «Совместимость: Revit 2020 / 2022 / 2024» и «Зависимости:», __title__/__author__) [MM001, MM002, MM004]; UTF-8 без BOM [MM003]; только CPython, IronPython для нового кода запрещён; консервативный синтаксис (без match/walrus — движок pyRevit может быть старше локального Python; чекер — статический гейт, runtime-истина — Revit UAT); импорты только из белого списка: stdlib + clr/System/Autodesk/pyrevit + MM LAB.extension/lib + vendored openpyxl/et_xmlfile — «без сторонних импортов» с ЯВНЫМ исключением для vendored lib/ [MM008]; запрет `from X import *` [MM009]; общие функции — в lib, дублирование в скриптах запрещено; параметры — через revit_compat.get_parameter (BuiltInParameter) или get_shared_parameter (GUID), НЕ LookupParameter со строковым литералом [MM010]; мультиверсия ТОЛЬКО через revit_compat, `if version >=` в скриптах запрещён (D-01); fail-fast: main() начинается с revit_compat.require_supported_version(COMMAND_NAME) (D-03); транзакции: Start/Commit в try, RollBack+raise в except; верхнеуровневая обработка ошибок с TaskDialog, голый except запрещён [MM011]; pyrevit.forms под CPython не работает — TaskDialog/WinForms [MM012]; README.md обязателен [MM006]; bundle.yaml с title/tooltip обязателен [MM005]; регистрация в layout panel-bundle.yaml обязательна [MM007]; без мусора в папке кнопки (__pycache__, *.pyc, *.csv, .vs) [MM013]; канонический sys.path-бутстрап — код-блок из interfaces дословно, другие формы (включая имя EXTENSION_ROOT и 4 уровня «..») запрещены [MM014] (D-15); doc/uidoc получать в script.py и передавать в функции параметрами (не тянуть __revit__ из lib-модулей).
    (4) `## Структура кнопки`: состав папки *.pushbutton (script.py, bundle.yaml, README.md, icon.png ~96×96 PNG); формат bundle.yaml кнопки (title ru/en_us, tooltip ru/en_us, author); правила layout panel-bundle.yaml (имена без суффикса .pushbutton, порядок записей = порядок кнопок, `---` (3+) — разделитель, `>>>` (3+) — slide-out; кнопка вне layout не видна на pyRevit ≤5.x); после добавления кнопки или правки lib — pyRevit Reload обязателен.
    (5) `## Совместимость Revit (2020 / 2022 / 2024)`: таблица ломающих изменений (Units: DisplayUnitType → ForgeTypeId/UnitTypeId, deprecated 2021, удалён 2022; Floor: NewFloor → Floor.Create(IList[CurveLoop]), удалён 2023; ElementId: IntegerValue Int32 → Value Int64 в 2024; pythonnet 3.x: строгие overload/enum-касты) и список функций revit_compat из interfaces с одной строкой описания каждой; на неподдерживаемой версии — fail-fast с перечнем поддержанных.
    (6) `## Проверка конвенции`: команды чекера — проверка кнопки (`py -3 tools/check_convention.py "<путь к .pushbutton>"`), сырого .py, всего репо (`--all --baseline tools/convention_baseline.json`), гейт для новых/адаптируемых (`--strict`); таблица кодов MM000–MM014 (код, severity, суть одной строкой — сверить со словарём RULES реализации; MM014 — канонический lib-бутстрап); семантика baseline (legacy заморожен; при адаптации кнопки её запись удаляется из baseline); тесты инструментов: `py -3 -m unittest discover -s tools/tests -q`.
    (7) `## Команды MM LAB`: таблица 7 команд (слаг → назначение одной строкой → файл agents/commands/mm-<слаг>.md); правило: пользовательская фраза «сохрани сессию» = выполнить процедуру /mm-save-session; все команды MM LAB имеют префикс mm- (D-19); канонические процедуры лежат в agents/commands/, адаптеры агентов — в .claude/commands/, .gemini/commands/, .kilo/commands/.
    (8) `## Git-регламент`: стейджинг ТОЛЬКО пофайловый с явными путями, `git add .` и `git add -A` запрещены (D-17); каждая сессия — отдельный коммит; сообщения на русском; шаблон сессионного коммита — код-блок ДОСЛОВНО из CONTEXT §specifics (заголовок «сессия: …», разделы «## Сессия» с полями «- Агент/модель:», «- Дата:», «- Изменено файлов:», «## Изменённые файлы», «## Результаты»); push только после подтверждения человеком (D-18); для кириллических путей — `git -c core.quotepath=false status --porcelain`.
    (9) `## Правило graphify` (D-21): если существует graphify-out/graph.json — вопросы о кодовой базе начинать с `graphify query "<вопрос>"` (`graphify path` для связей, `graphify explain` для концептов); после правок кода — `graphify update .`; если graphify-out/ нет — правило спит.
    (10) `## Obsidian-хранилище` (D-21): ./MMLabs_OBSIDIAN; при старте сессии читать 00-home/index.md и текущие приоритеты.md; при завершении сессии — процедура /mm-save-session (заметка в sessions/, обновление текущих приоритетов, knowledge/decisions|debugging при решениях/багах, обновление index.md).
    userEmail/currentDate НЕ упоминать (D-21). Полные тексты процедур mm-команд НЕ дублировать — только ссылки на agents/commands/.
  </action>
  <verify>
    <automated>py -3 -c "import io,sys; t=io.open('AGENTS.md',encoding='utf-8').read(); req=['## Обязательные правила кода','## Совместимость Revit','## Проверка конвенции','## Команды MM LAB','## Git-регламент','graphify','Obsidian','#! python3','MM013','MM014','revit_compat','mm-adopt-script','core.quotepath=false','_EXTENSION_DIR']; m=[r for r in req if r not in t]; sys.exit(0 if not m else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify завершается с кодом 0 (все обязательные маркеры на месте)
    - AGENTS.md ≥ 180 строк; язык — русский
    - Таблица правил содержит все коды MM000–MM014 включительно, severity каждого кода совпадает со словарём RULES в tools/check_convention.py
    - В тексте есть канонический бутстрап-блок с `_EXTENSION_DIR` и НЕТ имени `EXTENSION_ROOT`
    - Шаблон сессионного коммита содержит строки "## Сессия" и "- Агент/модель:" (новый утверждённый формат; старого заголовка "## Информация о сессии" в файле нет)
    - Подстрок "userEmail" и "currentDate" в файле нет
    - Каталог команд перечисляет все 7 слагов: mm-adopt-script, mm-new-button, mm-check, mm-save-session, mm-update-repo, mm-doctor, mm-new-compat
  </acceptance_criteria>
  <done>AGENTS.md — полный, самодостаточный стандарт, сверенный с реализацией чекера и compat</done>
</task>

<task type="auto">
  <name>Task 2: Тонкие указатели — CLAUDE.md, GEMINI.md, .kilocode/rules/00-mmlab.md</name>
  <files>CLAUDE.md, GEMINI.md, .kilocode/rules/00-mmlab.md</files>
  <read_first>
    - CLAUDE.md (текущее содержимое — судьба каждого блока: graphify → перенесён в AGENTS.md, здесь удалить; «GSD Release Map (Регламент 8.2.1 / 11.3)» → ОСТАВИТЬ дословно (Claude-специфика, GSD живёт в .claude); Obsidian-блок → заменён процедурой /mm-save-session в AGENTS.md, здесь удалить)
    - AGENTS.md (созданный в Task 1 — указатели ссылаются на него; убедиться, что переносимые правила там реально есть)
    - .planning/phases/03-convention/03-RESEARCH.md (§Pattern 1 — механика: Claude Code НЕ читает AGENTS.md сам, мост = строка @AGENTS.md; Gemini — memory import @AGENTS.md в GEMINI.md; Kilo читает AGENTS.md нативно, файл правил — совместимость со старыми версиями)
  </read_first>
  <action>
    (1) Переписать корневой CLAUDE.md: строка 1 — `@AGENTS.md`; далее короткий комментарий «Ниже — только специфичное для Claude Code; стандарт MM LAB — в AGENTS.md»; сохранить из старого содержимого ТОЛЬКО раздел «GSD Release Map (Регламент 8.2.1 / 11.3)» целиком (команда «Синхронизируй gsd», вызовы python3 RELEASE_MAP/gsd_release_sync.py check|sync-docs|sync, статусы «Не начато → В работе → Готово → Релиз», правило показать изменения); добавить строку: команды /mm-* доступны как slash-команды из .claude/commands/ (канонические процедуры — agents/commands/, каталог — AGENTS.md §Команды MM LAB). Разделы graphify и Obsidian из CLAUDE.md удалить полностью. Файл .claude/CLAUDE.md НЕ трогать (служебный: trigger graphify-скилла, userEmail, currentDate).
    (2) Создать GEMINI.md: строка 1 — `@AGENTS.md`; далее 1–2 строки: «Канонический стандарт MM LAB — AGENTS.md (импортирован выше). Команды /mm-* — в .gemini/commands/.»
    (3) Создать .kilocode/rules/00-mmlab.md: 2–3 строки: «Канонический стандарт MM LAB — файл AGENTS.md в корне репозитория; прочитай его перед работой (новые версии Kilo Code подхватывают AGENTS.md автоматически). Команды /mm-* — в .kilo/commands/.»
    Symlink не использовать (D-05, Windows). Кодировка всех файлов — UTF-8 без BOM.
  </action>
  <verify>
    <automated>py -3 -c "import io,sys; c=io.open('CLAUDE.md',encoding='utf-8').read(); g=io.open('GEMINI.md',encoding='utf-8').read(); k=io.open('.kilocode/rules/00-mmlab.md',encoding='utf-8').read(); ok=c.lstrip().startswith('@AGENTS.md') and ('gsd_release_sync.py' in c) and ('graphify query' not in c) and ('MMLabs_OBSIDIAN' not in c) and g.lstrip().startswith('@AGENTS.md') and ('AGENTS.md' in k); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - Команда verify завершается с кодом 0
    - CLAUDE.md: первая непустая строка == `@AGENTS.md`; раздел «GSD Release Map» с командой «Синхронизируй gsd» сохранён; подстрок "graphify query" и "MMLabs_OBSIDIAN" нет
    - GEMINI.md существует, первая непустая строка == `@AGENTS.md`
    - .kilocode/rules/00-mmlab.md существует и содержит "AGENTS.md"
    - .claude/CLAUDE.md не изменён (git diff по нему пуст)
    - Коммит: docs(03-05): AGENTS.md + тонкие указатели агентов
  </acceptance_criteria>
  <done>Три агента получают один и тот же стандарт: Claude через @import, Gemini через memory import, Kilo нативно (+ файл правил для старых версий)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| текст стандарта → поведение агентов | AGENTS.md загружается в контекст каждой сессии каждого агента и управляет их действиями (git, правки кода) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | T (Tampering) | Git-регламент в AGENTS.md | mitigate | текст явно запрещает git add ./-A и деструктивные операции; push только после подтверждения (D-17, D-18) — агенты наследуют запреты из контекста |
| T-03-14 | R (Repudiation) | потеря правил при переписывании CLAUDE.md | mitigate | acceptance criteria фиксируют сохранение GSD-блока и наличие перенесённых правил в AGENTS.md до удаления из CLAUDE.md (Task 2 читает AGENTS.md перед правкой) |
| T-03-15 | I (Info disclosure) | перенос served-контекста в публичный стандарт | mitigate | userEmail/currentDate в AGENTS.md не переносятся (D-21, проверяется acceptance criteria) |
</threat_model>

<verification>
- Обе automated-команды задач → exit 0
- `git -c core.quotepath=false status --porcelain` показывает изменения только в AGENTS.md, CLAUDE.md, GEMINI.md, .kilocode/rules/00-mmlab.md
</verification>

<success_criteria>
- Один канонический текст конвенции; три указателя суммарно ~10–15 строк, без дублей
- Правила graphify и Obsidian живут в AGENTS.md; GSD-блок остался в CLAUDE.md
- Таблица MM-кодов и список функций compat в стандарте совпадают с реализацией
</success_criteria>

## Artifacts this phase produces

Символы, создаваемые этим планом:
- Файл `AGENTS.md` (разделы: «Обязательные правила кода», «Структура кнопки», «Совместимость Revit (2020 / 2022 / 2024)», «Проверка конвенции», «Команды MM LAB», «Git-регламент», «Правило graphify», «Obsidian-хранилище»)
- Переписанный `CLAUDE.md` (строка `@AGENTS.md` + раздел «GSD Release Map»)
- Файл `GEMINI.md` (строка `@AGENTS.md`)
- Файл `.kilocode/rules/00-mmlab.md`

<output>
После завершения создать `.planning/phases/03-convention/03-05-SUMMARY.md` по шаблону summary.
</output>
