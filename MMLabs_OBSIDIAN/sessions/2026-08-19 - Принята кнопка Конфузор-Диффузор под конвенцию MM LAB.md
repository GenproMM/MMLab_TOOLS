# Сессия: 2026-08-19 — Принята кнопка Конфузор-Диффузор под конвенцию MM LAB

## Цель
Принять сторонний скрипт `Конфузор-Диффузор.pushbutton` (панель ИОС) в конвенцию MM LAB через процедуру `/mm-adopt-script`.

## Что сделано

### Адаптация скрипта
**Файл:** `MM_LAB.extension/MM Lab.tab/ИОС.panel/Конфузор-Диффузор.pushbutton/script.py`

Исправлены нарушения конвенции (код на русском, API-логика не менялась):
- **MM003** — файл пересохранён в UTF-8 без BOM (исходный файл начинался с BOM `EF BB BF`)
- **MM004** — добавлен docstring модуля с обязательными строками «Совместимость: Revit 2020 / 2022 / 2024» и «Зависимости: нет»
- **MM014** — неканонический lib-бутстрап (`SCRIPT_DIR`/`EXTENSION_ROOT`, `sys.path.append`) заменён на канонический блок (_SCRIPT_DIR/_EXTENSION_DIR/_LIB_DIR, sys.path.insert) по D-15
- **Правило 18** — `__revit__` извлечение перенесено из lib-модуля в локальный `_entry()` в script.py, doc передаётся параметром в main()
- **Правило 9 / D-03** — добавлен `revit_compat.require_supported_version(COMMAND_NAME)` в начале main() для fail-fast гейта версии Revit

### Метаданные кнопки
- **bundle.yaml:** добавлены en_us title/tooltip (зеркало existing ИОС-кнопок), author → "GENPRO LAB"
- **README.md:** создан с нуля — разделы Описание (что делает кнопка), Логика работы, Используемые параметры, Зависимости, Совместимость
- **icon.png:** перенесен из исходника (уже был)

### Регистрация
- **baseline:** запись `MM_LAB.extension/MM Lab.tab/ИОС.panel/Конфузор-Диффузор.pushbutton` удалена из `units` в `tools/convention_baseline.json` (снятие grandfathering)
- **STATE.md:** добавлена строка в таблицу "Quick Tasks Completed" для `260819-cfz`

## Проверки
- `py -3 tools/check_convention.py "...Конфузор-Диффузор.pushbutton" --strict` → **exit 0, 0 ошибок, 0 предупреждений**
- `py -3 tools/check_convention.py --all --baseline ...` → exit 1, но **только** от unrelated pre-existing кнопок (СНиП.pushbutton, СНиП_ФОП25.pushbutton на панели АРХИТЕКТУРА, вне baseline); кнопка Конфузор-Диффузор в нарушениях не участвует

## Результат
- Quick task `260819-cfz-adopt-konfuzor-diffuzor` заведён в `.planning/quick/`
- Коммит `9c8f988` создан пофайлово (7 файлов)
- Push не выполнялся (требует подтверждения пользователя, D-18)

## Связанные материалы
- **Процедура:** `agents/commands/mm-adopt-script.md`
- **Стандарт:** `AGENTS.md` (MM-коды, правила конвенции, процедуры команд)
- **Примеры:** соседние адаптированные кнопки ИОС — Доп расход 0/1, Сброс потерь

## Примечания
- Бизнес-логика кнопки (классификация конфузор/диффузор по площадям HVAC-коннекторов) не менялась — исправления исключительно конвенционные
- Вспомогательный пробел: кнопки СНиП/СНиП_ФОП25 на панели АРХИТЕКТУРА никогда не адаптировались и не в baseline; могут быть приняты отдельными `/mm-adopt-script` если нужно
