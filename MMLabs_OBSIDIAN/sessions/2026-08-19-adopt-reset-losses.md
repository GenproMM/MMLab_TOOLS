# 2026-08-19 — Приёмка кнопки «Сброс потерь» (ИОС)

**Сессия:** Адаптация кнопки «Сброс потерь» под конвенцию MM LAB через процедуру `/mm-adopt-script`. Кнопка уже лежала в `ИОС.panel/Сброс потерь.pushbutton` с нарушениями MM004, MM014.

**Статус:** Перепройдена процедура приёмки до РЕВЬЮ-ГЕЙТА включительно, адаптация одобрена, осталось регистрация и коммит (но сессия завершена до этого).

## Что сделано

1. **Исходный анализ** — чекер показал 4 warning (MM004, MM014×3).
2. **Адаптация `script.py`:**
   - Добавлен docstring с «Совместимость» и «Зависимости» (MM004).
   - Канонический lib-бутстрап `_SCRIPT_DIR/_EXTENSION_DIR/_LIB_DIR` вместо легаси `EXTENSION_ROOT` (MM014, D-15).
   - Добавлены `main(doc)` + `_entry()` (вынесена логика, `doc` теперь параметр — правило 18).
   - `revit_compat.require_supported_version()` в начало `main()` (fail-fast D-03).
   - `__title__` переведён на формат с `\n`.
   - `__author__` → `"GENPRO LAB"`.
   - Верхнеуровневый `try/except` дополнен `except SystemExit: pass`.
3. **bundle.yaml** — добавлены `en_us` для `title`/`tooltip`, `author` → `"GENPRO LAB"`.
4. **README.md** — создан по стандартному шаблону (разделы Описание, Логика, Параметры, Зависимости, Совместимость, Ограничения).
5. **Гейт-проверка** — `check_convention.py --strict` → чисто (0 ошибок, 0 предупреждений).
6. **РЕВЬЮ-ГЕЙТ** — показана сводка адаптации с MM-кодами и полный diff (см. ниже).

## Файлы сессии

- `MM_LAB.extension/MM Lab.tab/ИОС.panel/Сброс потерь.pushbutton/script.py` (адаптирован)
- `MM_LAB.extension/MM Lab.tab/ИОС.panel/Сброс потерь.pushbutton/bundle.yaml` (обновлён)
- `MM_LAB.extension/MM Lab.tab/ИОС.panel/Сброс потерь.pushbutton/README.md` (создан)

## Ключевые моменты

- Кнопка лежала в репозитории с записью в `tools/convention_baseline.json` (`units`: MM004, MM006, MM014).
- Панель (ИОС) и имя кнопки согласованы на шаге 3–4 процедуры.
- Адаптированный код структурно идентичен другим принятым кнопкам ИОС (`Доп расход 1` и аналогичным).
- Гейт `--strict` успешно пройден после адаптации; регистрация и коммит ещё впереди.

## Ссылки

- Процедура: [[agents/commands/mm-adopt-script.md]]
- Стандарт кода: [[AGENTS.md]] §Обязательные правила кода, §Проверка конвенции
- Конвенция кнопок: [[AGENTS.md]] §Структура кнопки
