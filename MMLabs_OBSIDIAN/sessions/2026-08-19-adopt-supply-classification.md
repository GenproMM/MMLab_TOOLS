# Сессия: Приёмка кнопки «Приточный по классификации»

**Дата:** 2026-08-19  
**Агент:** Claude Code / Claude Sonnet 5  
**Тип:** Приёмка стороннего кода (адаптация под конвенцию)

## Что делалось

Полная адаптация кнопки **«Приточный по классификации»** из легаси-статуса (legacy-допуск в baseline) в соответствие с конвенцией MM LAB. Процедура: чекер → адаптация → ревью-гейт → регистрация → коммит.

### Исходное состояние

Папка: `MM_LAB.extension/MM Lab.tab/ИОС.panel/Приточный по классификации.pushbutton/`

Нарушения конвенции:
- **MM003** — файл с UTF-8 BOM (байты EF BB BF в начале)
- **MM004** — отсутствовал docstring модуля с «Совместимость:» и «Зависимости:»
- **MM006** — отсутствовал README.md
- **MM014** — нестандартный lib-бутстрап: переменные `SCRIPT_DIR`/`EXTENSION_ROOT`, `sys.path.append()` вместо канонического `_SCRIPT_DIR`/`_EXTENSION_DIR`/`sys.path.insert(0, ...)`
- **Разнобой имён**: папка «Приточный по классификации», bundle.yaml — «Приточный по системе», script.py — `__title__ = "Вытяжка"` (очевидно скопировано от другой кнопки)

### Адаптация

**script.py:**
- Удален UTF-8 BOM
- Добавлен настоящий docstring с совместимостью и зависимостями
- Заменён lib-бутстрап на канонический (D-15)
- `__title__` → «Приточный\nпо классификации»
- `COMMAND_NAME` синхронизирован с `__title__`
- `__author__` → «GENPRO LAB»
- Вынесена логика в `main(doc)` + `_entry()` — doc/uidoc получаются в script.py (правило 18)
- Добавлен `revit_compat.require_supported_version()` в начало `main()` (fail-fast по версии)
- Отмена диалога → просто `return` (вместо `raise SystemExit`)
- Убраны неиспользуемые импорты `get_document`/`show_error` из lib

**bundle.yaml:**
- Добавлены локализации en_us
- Русский title синхронизирован с новым именем
- author → «GENPRO LAB»

**README.md:** создан с разделами (Описание, Логика работы, Используемые параметры, Зависимости, Совместимость, Ограничения)

**tools/convention_baseline.json:** удалена запись из `units` — дальше кнопка держит `--strict` без поблажек

### Проверка

- Strict check (`--strict`) — **0 ошибок, 0 предупреждений** ✅
- Full-repo baseline check — **pass** ✅
- Регистрация в panel layout — **уже на месте** (2-я позиция в ИОС.panel)

## Ключевые файлы

| Файл | Статус |
|------|--------|
| `MM_LAB.extension/MM Lab.tab/ИОС.panel/Приточный по классификации.pushbutton/script.py` | ✅ Переписан (MM001–MM004, MM014) |
| `MM_LAB.extension/MM Lab.tab/ИОС.panel/Приточный по классификации.pushbutton/bundle.yaml` | ✅ Обновлен (локализации) |
| `MM_LAB.extension/MM Lab.tab/ИОС.panel/Приточный по классификации.pushbutton/README.md` | ✅ Создан (MM006) |
| `tools/convention_baseline.json` | ✅ Обновлен (удалена запись из units) |
| `.planning/STATE.md` | ✅ Обновлен (добавлена запись в Quick Tasks) |
| `.planning/quick/260819-ptc-adopt-supply-classification/` | ✅ Создана директория с PLAN и SUMMARY |

## Commit

**Hash:** `a3493e4`  
**Сообщение:** сессия: приёмка кнопки «Приточный по классификации», адаптация под конвенцию MM LAB  
**Файлов:** 7  
**Статус:** Локальный, push не выполнялся

## Сношение с конвенцией AGENTS.md

Вся работа выполнена в точном соответствии с процедурой `/mm-adopt-script` из `agents/commands/mm-adopt-script.md`:
- Шаг 1: Входные ограничения безопасности (не исполнять, только читать) ✅
- Шаг 2: Исходный отчёт чекера ✅
- Шаг 3: Выбор панели (ИОС) ✅
- Шаг 4: Согласование имени кнопки ✅
- Шаг 5: Адаптация под конвенцию (шапка, lib-бутстрап, импорты, структура) ✅
- Шаг 6: Гейт-проверка чекером (`--strict`) ✅
- Шаг 7: РЕВЬЮ-ГЕЙТ (сводка правок + одобрение) ✅
- Шаг 8: Регистрация (baseline, layout) ✅
- Шаг 9: Quick task GSD ✅
- Шаг 10: Коммит (пофайловый стейджинг, обязательный шаблон) ✅
- Шаг 11: Напоминание о pyRevit Reload ✅

## Следующие шаги

1. **pyRevit Reload** или перезапуск Revit — расширение парсится на старте
2. **UAT в Revit** — проверить, что кнопка видна на панели ИОС и работает корректно
3. **Push** — выполнить `git push` после подтверждения пользователем
