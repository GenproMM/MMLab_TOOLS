---
tags: [sessions, log, debugging, ios]
date: 2026-07-09
---

# Устранены ошибки кнопок панели ИОС

Сделано:
- Устранён краш кнопки «Сброс потерь»: удалены несуществующие `RBS_DUCT_LOSS_METHOD_SERVER_PARAM` и `RBS_DUCT_TERMINAL_LOSS_METHOD_SERVER_PARAM` в `get_loss_method_parameters`, оставлен единственный валидный `RBS_DUCT_FITTING_LOSS_METHOD_SERVER_PARAM` (покрывает fitting и accessory).
- Исправлено рассогласование контракта: `ensure_loss_method_undefined` возвращала bool, а `Сброс потерь/script.py` ждал строки `"updated"`/`"already"` — теперь возвращается корректный tri-state.
- Подтверждено, что ошибка «Доп. расход = 1» (`get_Parameter TypeError`) уже была устранена прежними коммитами (d207200, 7327da9, 0acdac0) переходом на `LookupParameter(string)`.
- Оформлена быстрая GSD-задача 260709-jko (PLAN + SUMMARY), обновлён STATE.md.

Контекст:
- Все правки в общей библиотеке `MM_LAB.extension/lib/ios_common_helpers.py`; сами скрипты кнопок не менялись.
- Живая проверка в Revit не выполнялась (headless-среда) — оставлен follow-up на smoke UAT.

Следующие шаги:
- [[Несуществующие имена BuiltInParameter ломают кнопки ИОС при вызове]]
- [[Отсутствие тестов повышает риск регрессий при правках]]
- [[Текущие приоритеты сосредоточены на надежности ИОС-скриптов]]
